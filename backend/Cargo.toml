[package]
name = "otel-ui-backend"
version = "0.1.0"
edition = "2021"

[workspace]

[[bin]]
name = "otel-ui-backend"
path = "src/main.rs"

[lib]
name = "otel_ui_backend"
path = "src/lib.rs"

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"

# WebSocket
axum = { version = "0.8", features = ["ws"] }
tower-http = { version = "0.6", features = ["cors"] }

# OTLP gRPC server (receive spans from collector)
tonic = { version = "0.14.5", features = ["gzip"] }
opentelemetry-proto = { version = "0.31", features = ["gen-tonic", "trace", "metrics", "logs"] }
prost = "0.14"
bytes = "1"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Tracing / Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# HTTP client for dashboard proxy
reqwest = "0.12"

# Utilities
uuid = { version = "1", features = ["v4"] }
dashmap = "6"
futures-util = "0.3"
chrono = { version = "0.4", features = ["serde"] }
base64 = "0.22"
hex = "0.4"
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
rusqlite = { version = "0.39.0", features = ["bundled"] }
